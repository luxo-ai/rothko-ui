const inputProps = [
  {
    name: 'error',
    type: 'boolean',
    defaultValue: 'false',
    description: 'Specifies whether there is an error with the input.',
  },
] as const;

export default inputProps;
