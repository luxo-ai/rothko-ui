import type { CardCopy } from '../types';

const autocompleteCopy: CardCopy = {
  title: 'Autocomplete',
  description: 'Autocomplete component is a text input that shows suggestions as you type.',
};

export default autocompleteCopy;
