const textareaProps = [
  {
    name: 'error',
    type: 'boolean',
    defaultValue: 'false',
    description: 'Specifies whether there is an error with the textarea',
  },
] as const;

export default textareaProps;
