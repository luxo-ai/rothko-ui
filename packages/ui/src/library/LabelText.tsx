import styled from 'styled-components';
import Typography from '../components/Typography/Typography';

const LabelText = styled(Typography.label).attrs({ light: true })`
  margin-bottom: 0.25rem;
`;

export default LabelText;
